const generateParams = require("../dev-params")(undefined, "fundracer-splits.js");

contract("FundRace Splits", () => {

    it("should run a test", async () => {
        assert(true, "Logic is broken");
    });
});